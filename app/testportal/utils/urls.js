import {
  assessmentSocketUrl as socketUrl,
  assessment_proctoring_url as proctoringUrl,
  assessmentGqlUrl as gqlUrl,
  studentUrl,
  aiUrl,
  restUrl as awsUrl,
} from "@/config/urls";

export { socketUrl, proctoringUrl, gqlUrl, studentUrl, aiUrl, awsUrl };
